name: vrv_desk
description: "VrV Desk - Ultra-Lightweight Screen Mirror & Remote Control"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  flutter_rust_bridge: ^2.0.0
  mobile_scanner: ^5.1.1
  qr_flutter: ^4.1.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
